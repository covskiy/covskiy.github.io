# План: IntroAnimation → SplashPage

## Цель

Переименовать компонент `IntroAnimation` в полноценную страницу `SplashPage` с роутингом.

## Роутинг

| Путь | Компонент | Примечания |
|------|-----------|-----------|
| `/` | SplashPage | Проигрывает анимацию 1 раз, затем редирект на `/home` |
| `/home` | HomePage | Еagerly loaded |
| `/about` | AboutPage | Lazy loaded |
| `/services` | ServicesPage | Lazy loaded |
| `/contact` | ContactPage | Lazy loaded |

## Хранилище состояния

- **localStorage** с ключом `splashShown`
- Если `localStorage.getItem('splashShown') === 'true'` — сразу редирект на `/home`
- После завершения анимации — `localStorage.setItem('splashShown', 'true')`

## Поведение блокировки UI

**Важно:** SplashPage **должен блокировать** отрисовку navbar и остального UI до завершения анимации.

### Как это работает:

1. При переходе на `/` — App.tsx рендерит **только** SplashPage (без VerticalNav, BurgerMenu, Routes)
2. SplashPage:
   - Проверяет localStorage
   - Если `splashShown === 'true'` — сразу редирект на `/home`
   - Иначе — проигрывает анимацию
   - После завершения — редирект на `/home` + записывает `splashShown: true`
3. При переходе на `/home` и другие пути — App.tsx рендерит полностью (VerticalNav + Routes)

### Реализация в App.tsx:

```tsx
import { useState, useEffect } from 'react';
import { Routes, Route, useLocation, useNavigate } from 'react-router';
import SplashPage from './pages/SplashPage/SplashPage';
import VerticalNav from './components/VerticalNav/VerticalNav';
import BurgerMenu from './components/BurgerMenu/BurgerMenu';
import PageTransition from './components/PageTransition/PageTransition';
import { routes } from './routes';
import styles from './App.module.css';

function App() {
  const location = useLocation();
  const navigate = useNavigate();
  const [splashComplete, setSplashComplete] = useState(false);

  useEffect(() => {
    if (location.pathname === '/home') {
      setSplashComplete(true);
    }
  }, [location.pathname]);

  const handleSplashComplete = () => {
    setSplashComplete(true);
    navigate('/home', { replace: true });
  };

  const showSplash = location.pathname === '/' && !splashComplete;

  return (
    <div className={styles.app}>
      {showSplash ? (
        <SplashPage onComplete={handleSplashComplete} />
      ) : (
        <>
          <VerticalNav />
          <BurgerMenu />
          <main className={styles.main}>
            <Routes>
              {routes.map(({ path, element }) => (
                <Route
                  key={path}
                  path={path}
                  element={<PageTransition>{element}</PageTransition>}
                />
              ))}
            </Routes>
          </main>
        </>
      )}
    </div>
  );
}
```

### Что нужно для SplashPage:

- Проп `onComplete` — callback для уведомления App о завершении
- Логика localStorage — проверка при монтировании, запись при завершении
- Внутренний таймер/transition для плавного перехода на /home

## Список задач

### 1. Реструктуризация файлов

- [ ] 1.1 Создать папку `src/pages/SplashPage/`
- [ ] 1.2 Переместить `IntroAnimation.tsx` → `src/pages/SplashPage/SplashPage.tsx`
- [ ] 1.3 Переместить `IntroAnimation.module.css` → `src/pages/SplashPage/SplashPage.module.css`
- [ ] 1.4 Удалить папку `src/components/IntroAnimation/`

### 2. Обновление SplashPage.tsx

- [ ] 2.1 Переименовать экспорт `IntroAnimation` → `SplashPage`
- [ ] 2.2 Обновить импорт CSS
- [ ] 2.3 Добавить проп `onComplete` (required)
- [ ] 2.4 Добавить логику localStorage:
  - `localStorage.getItem('splashShown')` при монтировании
  - Если `true` — сразу вызвать `onComplete()`
  - Если `false` — проиграть анимацию, по завершении:
    - `localStorage.setItem('splashShown', 'true')`
    - вызвать `onComplete()`

### 3. Обновление routes.tsx

- [ ] 3.1 HomePage остаётся на `/home` (eagerly loaded)
- [ ] 3.2 Роут `/` обрабатывается в App.tsx (conditional rendering)

### 4. Обновление App.tsx

- [ ] 4.1 Импортировать SplashPage из `./pages/SplashPage/SplashPage`
- [ ] 4.2 Добавить `useLocation`, `useNavigate` хуки
- [ ] 4.3 Добавить state `splashComplete`
- [ ] 4.4 Добавить callback `handleSplashComplete`
- [ ] 4.5 Использовать conditional rendering на основе location.pathname

### 5. Обновление документации

- [ ] 5.1 Обновить `docs/ARCHITECTURE.md`:
  - Удалить раздел про `IntroAnimation` в компонентах
  - Добавить `SplashPage/` в секцию pages
  - Обновить схему роутинга
- [ ] 5.2 Обновить дерево папок в начале документа
- [ ] 5.3 Обновить схему загрузки приложения

## Ожидаемые изменения в дереве папок

```
src/
├── components/
│   ├── ... (IntroAnimation удалён)
│   └── ...
├── pages/
│   ├── SplashPage/          # НОВЫЙ
│   │   ├── SplashPage.tsx
│   │   └── SplashPage.module.css
│   ├── HomePage/
│   │   ├── HomePage.tsx
│   │   └── HomePage.module.css
│   └── ...
```

## Итоговый routes.tsx

```tsx
// HomePage остаётся eagerly loaded, без изменений в структуре routes
import HomePage from './pages/HomePage/HomePage';

const AboutPage = lazy(() => import('./pages/AboutPage/AboutPage'));
const ServicesPage = lazy(() => import('./pages/ServicesPage/ServicesPage'));
const ContactPage = lazy(() => import('./pages/ContactPage/ContactPage'));

export const routes: RouteObject[] = [
  { path: '/home', element: <HomePage /> },
  { path: '/about', element: withSuspense(<AboutPage />) },
  { path: '/services', element: withSuspense(<ServicesPage />) },
  { path: '/contact', element: withSuspense(<ContactPage />) },
];

// Примечание: роут `/` обрабатывается в App.tsx (conditional rendering)
```

## Итоговый App.tsx

```tsx
import { useState, useEffect } from 'react';
import { Routes, Route, useLocation, useNavigate } from 'react-router';
import SplashPage from './pages/SplashPage/SplashPage';
import VerticalNav from './components/VerticalNav/VerticalNav';
import BurgerMenu from './components/BurgerMenu/BurgerMenu';
import PageTransition from './components/PageTransition/PageTransition';
import { routes } from './routes';
import styles from './App.module.css';

function App() {
  const location = useLocation();
  const navigate = useNavigate();
  const [splashComplete, setSplashComplete] = useState(false);

  useEffect(() => {
    if (location.pathname === '/home') {
      setSplashComplete(true);
    }
  }, [location.pathname]);

  const handleSplashComplete = () => {
    setSplashComplete(true);
    navigate('/home', { replace: true });
  };

  const showSplash = location.pathname === '/' && !splashComplete;

  return (
    <div className={styles.app}>
      {showSplash ? (
        <SplashPage onComplete={handleSplashComplete} />
      ) : (
        <>
          <VerticalNav />
          <BurgerMenu />
          <main className={styles.main}>
            <Routes>
              {routes.map(({ path, element }) => (
                <Route
                  key={path}
                  path={path}
                  element={<PageTransition>{element}</PageTransition>}
                />
              ))}
            </Routes>
          </main>
        </>
      )}
    </div>
  );
}
```

## Примечание

- **SplashPage блокирует UI** до завершения анимации
- При `/` — показан только SplashPage
- При `/home` и др. — полный UI (VerticalNav + Routes)
- `splashShown` в localStorage — показывать анимацию 1 раз
- Старые ссылки на `/` (deep links) продолжат работать благодаря 404.html redirect